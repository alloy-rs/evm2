[alias]
cl = "clippy --workspace --features jit --all-targets"
docs = "doc --workspace --features jit --no-deps --document-private-items"
cli = "nextest run --bin evm2 --features jit"
t = "nextest run --workspace --features jit"
st = "nextest run --workspace --features jit --ignore-default-filter"
