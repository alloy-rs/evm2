[profile.default]
slow-timeout = { period = "60s", terminate-after = 1 }
status-level = "leak"

[[profile.default.overrides]]
filter = "test(statetest::)"
slow-timeout = { period = "600s", terminate-after = 1 }
