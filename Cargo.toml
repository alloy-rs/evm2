[package]
name = "evm2"
version = "0.1.0"
edition = "2024"

[dependencies]

[features]
default = ["std", "nightly"]
std = []

nightly = []

[profile.release]
opt-level = 3
panic = "abort"
# lto = "thin"
