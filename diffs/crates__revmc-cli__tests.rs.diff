--- upstream/crates/revmc-cli/tests.rs
+++ evm2/crates/jit/cli/tests.rs
@@ -3,6 +3,6 @@
 const CMD: &str = env!("CARGO_BIN_EXE_revmc");
 
 fn main() {
-    let code = revmc_cli_tests::run_tests(CMD.as_ref());
+    let code = evm2_jit_cli_tests::run_tests(CMD.as_ref());
     std::process::exit(code);
 }
