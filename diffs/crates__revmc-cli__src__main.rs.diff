--- upstream/crates/revmc-cli/src/main.rs
+++ evm2/crates/jit/cli/src/main.rs
@@ -21,7 +21,7 @@
 }
 
 fn main() -> Result<()> {
-    if revmc::runtime::maybe_run_jit_helper()?.is_break() {
+    if evm2_jit::runtime::maybe_run_jit_helper()?.is_break() {
         return Ok(());
     }
     if std::env::var_os("RUST_BACKTRACE").is_none() {
@@ -42,7 +42,7 @@
 fn init_tracing_subscriber() -> Result<()> {
     use tracing_subscriber::prelude::*;
 
-    let (profile_layer, is_profiling) = match std::env::var("REVMC_PROFILE").as_deref() {
+    let (profile_layer, is_profiling) = match std::env::var("EVM2_JIT_PROFILE").as_deref() {
         Ok("tracy") => {
             if !cfg!(feature = "tracy") {
                 return Err(eyre!("tracy profiler support is not compiled in"));
