--- upstream/crates/revmc-codegen/Cargo.toml
+++ evm2/crates/jit/codegen/Cargo.toml
@@ -1,7 +1,7 @@
 [package]
-name = "revmc-codegen"
+name = "evm2-jit-codegen"
 description = "EVM bytecode compiler code generation"
-homepage = "https://github.com/danipopes/revmc/tree/main/crates/revmc-codegen"
+homepage = "https://github.com/danipopes/evm2-jit/tree/main/crates/evm2-jit-codegen"

 version.workspace = true
 authors.workspace = true
@@ -21,22 +21,19 @@
 workspace = true

 [dependencies]
-revmc-backend.workspace = true
-revmc-builtins = { workspace = true, features = ["ir"] }
-revmc-context = { workspace = true, features = ["evm"] }
-revmc-llvm = { workspace = true, optional = true }
+evm2 = { workspace = true, features = ["parse"] }
+evm2-jit-backend.workspace = true
+evm2-jit-builtins = { workspace = true, features = ["ir"] }
+evm2-jit-context.workspace = true
+evm2-jit-llvm = { workspace = true, optional = true }

 alloy-primitives = { workspace = true, features = ["std", "map-fxhash"] }

 revm-bytecode.workspace = true
 revm-context-interface.workspace = true
-revm-context.workspace = true
-revm-database-interface.workspace = true
-revm-handler.workspace = true
-revm-inspector.workspace = true
 revm-interpreter.workspace = true
 revm-primitives.workspace = true
-revm-state.workspace = true
+revm-state = { workspace = true, optional = true }

 bitflags = "2.5"
 derive_more = { version = "2", default-features = false, features = ["debug"] }
@@ -54,13 +51,13 @@
 indexmap = "2.13.0"

 [build-dependencies]
-revmc-build.workspace = true
+evm2-jit-build.workspace = true

 [dev-dependencies]
-revmc-context.workspace = true
-revmc-statetest = { path = "../revmc-statetest" }
+evm2-jit-context = { workspace = true, features = ["evm2"] }

 paste.workspace = true
+revm-state.workspace = true
 similar-asserts = "2.0"
 snapbox = "1.1"
 serde_json = "1.0"
@@ -68,11 +65,17 @@

 [features]
 default = ["llvm"]
-llvm = ["dep:revmc-llvm"]
-llvm-prefer-static = ["llvm", "revmc-llvm?/prefer-static"]
-llvm-prefer-dynamic = ["llvm", "revmc-llvm?/prefer-dynamic"]
+llvm = ["dep:evm2-jit-llvm"]
+llvm-prefer-static = ["llvm", "evm2-jit-llvm?/prefer-static"]
+llvm-prefer-dynamic = ["llvm", "evm2-jit-llvm?/prefer-dynamic"]

 asm-keccak = ["alloy-primitives/asm-keccak"]

 # Internal features.
-__fuzzing = ["dep:arbitrary", "dep:paste", "dep:similar-asserts", "dep:tracing-subscriber"]
+__fuzzing = [
+    "dep:arbitrary",
+    "dep:paste",
+    "dep:revm-state",
+    "dep:similar-asserts",
+    "dep:tracing-subscriber",
+]
