From 0412a6ca8e59ed549bd8402e8039df85af1ae28d Mon Sep 17 00:00:00 2001
From: 0xalpharush <0xalpharush@protonmail.com>
Date: Thu, 9 Jul 2026 22:51:26 +0000
Subject: [PATCH] fix amsterdam runtime gas handling

---
 crates/evm2/src/ethereum/eip1559.rs           |  18 +-
 crates/evm2/src/ethereum/eip2930.rs           |  18 +-
 crates/evm2/src/ethereum/eip4844.rs           |   1 +
 crates/evm2/src/ethereum/eip7702.rs           | 200 ++++++++++++++++--
 crates/evm2/src/ethereum/legacy.rs            |  18 +-
 crates/evm2/src/ethereum/mod.rs               | 132 +++++++++++-
 crates/evm2/src/evm/mod.rs                    |  47 +++-
 crates/evm2/src/evm/system.rs                 |   1 +
 .../src/interpreter/instructions/system.rs    |   2 +
 crates/evm2/src/interpreter/message.rs        |   5 +
 crates/jit/builtins/src/lib.rs                |   2 +
 11 files changed, 412 insertions(+), 32 deletions(-)

diff --git a/crates/evm2/src/ethereum/eip1559.rs b/crates/evm2/src/ethereum/eip1559.rs
index 4d729dd..baf9c02 100644
--- a/crates/evm2/src/ethereum/eip1559.rs
+++ b/crates/evm2/src/ethereum/eip1559.rs
@@ -43,7 +43,13 @@ pub(super) fn handle<T: EvmTypes>(
         access_list_storage_keys,
         tx.value,
     );
-    let initial_state_gas = create_initial_state_gas(req.host.version(), tx.to.is_create());
+    let create_state_gas = create_initial_state_gas(req.host.version(), tx.to.is_create());
+    let (initial_state_gas, first_frame_state_gas) =
+        if req.host.feature(crate::EvmFeatures::EIP2780) {
+            (0, create_state_gas)
+        } else {
+            (create_state_gas, 0)
+        };
     validate_intrinsic_gas(tx.gas_limit, intrinsic, initial_state_gas)?;
     let floor_gas =
         floor_gas(req.host.version(), &tx.input, access_list_accounts, access_list_storage_keys);
@@ -75,7 +81,15 @@ pub(super) fn handle<T: EvmTypes>(
         ..TxEnv::default()
     };
     let (bytecode, mut message) = initial_message(
-        req.host, caller, tx.nonce, tx.to, &tx.input, tx.value, gas_limit, reservoir,
+        req.host,
+        caller,
+        tx.nonce,
+        tx.to,
+        &tx.input,
+        tx.value,
+        gas_limit,
+        reservoir,
+        first_frame_state_gas,
     )?;
     let mut result = req.host.execute_message(&tx_env, bytecode, &mut message);
     rollback_failed_execution(req.host, execution_checkpoint, &mut result);
diff --git a/crates/evm2/src/ethereum/eip2930.rs b/crates/evm2/src/ethereum/eip2930.rs
index 78a2171..a95f8ac 100644
--- a/crates/evm2/src/ethereum/eip2930.rs
+++ b/crates/evm2/src/ethereum/eip2930.rs
@@ -39,7 +39,13 @@ pub(super) fn handle<T: EvmTypes>(
         access_list_storage_keys,
         tx.value,
     );
-    let initial_state_gas = create_initial_state_gas(req.host.version(), tx.to.is_create());
+    let create_state_gas = create_initial_state_gas(req.host.version(), tx.to.is_create());
+    let (initial_state_gas, first_frame_state_gas) =
+        if req.host.feature(crate::EvmFeatures::EIP2780) {
+            (0, create_state_gas)
+        } else {
+            (create_state_gas, 0)
+        };
     validate_intrinsic_gas(tx.gas_limit, intrinsic, initial_state_gas)?;
     let floor_gas =
         floor_gas(req.host.version(), &tx.input, access_list_accounts, access_list_storage_keys);
@@ -70,7 +76,15 @@ pub(super) fn handle<T: EvmTypes>(
         ..TxEnv::default()
     };
     let (bytecode, mut message) = initial_message(
-        req.host, caller, tx.nonce, tx.to, &tx.input, tx.value, gas_limit, reservoir,
+        req.host,
+        caller,
+        tx.nonce,
+        tx.to,
+        &tx.input,
+        tx.value,
+        gas_limit,
+        reservoir,
+        first_frame_state_gas,
     )?;
     let mut result = req.host.execute_message(&tx_env, bytecode, &mut message);
     rollback_failed_execution(req.host, execution_checkpoint, &mut result);
diff --git a/crates/evm2/src/ethereum/eip4844.rs b/crates/evm2/src/ethereum/eip4844.rs
index b9a8154..6b36f9a 100644
--- a/crates/evm2/src/ethereum/eip4844.rs
+++ b/crates/evm2/src/ethereum/eip4844.rs
@@ -99,6 +99,7 @@ pub(super) fn handle<T: EvmTypes>(
         tx.value,
         gas_limit,
         reservoir,
+        0,
     )?;
     let mut result = req.host.execute_message(&tx_env, bytecode, &mut message);
     rollback_failed_execution(req.host, execution_checkpoint, &mut result);
diff --git a/crates/evm2/src/ethereum/eip7702.rs b/crates/evm2/src/ethereum/eip7702.rs
index ffab1c3..6c5a6e8 100644
--- a/crates/evm2/src/ethereum/eip7702.rs
+++ b/crates/evm2/src/ethereum/eip7702.rs
@@ -10,12 +10,12 @@ use crate::{
     Evm, EvmFeatures, EvmTypes, TxResult,
     env::TxEnv,
     evm::error_handler,
-    interpreter::Host,
+    interpreter::{GasTracker, Host, InstrStop, MessageResult},
     registry::{HandlerError, HandlerResult, TxRequest},
     version::GasId,
 };
 use alloy_consensus::transaction::Recovered;
-use alloy_primitives::U256;
+use alloy_primitives::{Address, TxKind, U256, map::AddressHashSet};
 
 pub(super) fn handle<T: EvmTypes>(
     req: TxRequest<'_, '_, T, Recovered<super::LazyTxEip7702>>,
@@ -47,10 +47,13 @@ pub(super) fn handle<T: EvmTypes>(
         access_list_storage_keys,
         tx.value,
     ) + eip7702_authorization_gas(req.host, tx.authorization_list.len());
-    // EIP-8037: per-auth state gas (account + bytecode) is charged before execution. Zero before
-    // Amsterdam.
-    let initial_state_gas = eip7702_authorization_state_gas(req.host, tx.authorization_list.len());
-    validate_intrinsic_gas(tx.gas_limit, intrinsic, initial_state_gas)?;
+    let is_eip2780 = req.host.feature(EvmFeatures::EIP2780);
+    let pre_execution_state_gas = if is_eip2780 {
+        0
+    } else {
+        eip7702_authorization_state_gas(req.host, tx.authorization_list.len())
+    };
+    validate_intrinsic_gas(tx.gas_limit, intrinsic, pre_execution_state_gas)?;
     let floor_gas =
         floor_gas(req.host.version(), &tx.input, access_list_accounts, access_list_storage_keys);
     validate_floor_gas(tx.gas_limit, floor_gas)?;
@@ -66,18 +69,55 @@ pub(super) fn handle<T: EvmTypes>(
     charge_upfront(req.host, caller, effective_gas_cost)?;
     req.host.state.account(&caller, false).map_err(error_handler!(req.host))?.bump_nonce();
     let chain_id = req.host.version().chain_id;
-    let (state_refund, regular_refund) =
-        apply_auth_list(req.host, chain_id, &tx.authorization_list)?;
+
+    let mut state_refund = 0;
+    let mut regular_refund = 0;
+    let settlement_initial_state_gas;
+    let (gas_limit, reservoir) = if is_eip2780 {
+        let (gas_limit, reservoir) =
+            initial_gas_and_reservoir(req.host.version(), tx.gas_limit, intrinsic, 0, 0);
+        let mut runtime_gas = GasTracker::new_with_regular_gas_and_reservoir(gas_limit, reservoir);
+        let auth_checkpoint = req.host.state.checkpoint();
+        let auth_gas = apply_auth_list_eip2780(
+            req.host,
+            chain_id,
+            &tx.authorization_list,
+            caller,
+            tx.to.into(),
+            tx.value,
+            &mut runtime_gas,
+        )?;
+        if auth_gas.out_of_gas {
+            req.host.state.rollback(auth_checkpoint, req.host.version().features);
+            return settle_gas(
+                req.host,
+                caller,
+                gas_price,
+                tx.gas_limit,
+                floor_gas,
+                0,
+                0,
+                false,
+                runtime_oog_result(tx.gas_limit, reservoir),
+            );
+        }
+        settlement_initial_state_gas = auth_gas.state_gas;
+        (runtime_gas.remaining(), runtime_gas.reservoir())
+    } else {
+        (state_refund, regular_refund) =
+            apply_auth_list(req.host, chain_id, &tx.authorization_list)?;
+        settlement_initial_state_gas = pre_execution_state_gas;
+        initial_gas_and_reservoir(
+            req.host.version(),
+            tx.gas_limit,
+            intrinsic,
+            pre_execution_state_gas,
+            state_refund,
+        )
+    };
     let execution_checkpoint = req.host.state.checkpoint();
 
     // EIP-7702 transactions are always calls, never creates.
-    let (gas_limit, reservoir) = initial_gas_and_reservoir(
-        req.host.version(),
-        tx.gas_limit,
-        intrinsic,
-        initial_state_gas,
-        state_refund,
-    );
     let tx_env = TxEnv {
         origin: caller,
         gas_price,
@@ -93,12 +133,15 @@ pub(super) fn handle<T: EvmTypes>(
         tx.value,
         gas_limit,
         reservoir,
+        0,
     )?;
     let mut result = req.host.execute_message(&tx_env, bytecode, &mut message);
     rollback_failed_execution(req.host, execution_checkpoint, &mut result);
-    result.gas.set_refunded(
-        result.gas.refunded().saturating_add(i64::try_from(regular_refund).unwrap_or(i64::MAX)),
-    );
+    if regular_refund != 0 {
+        result.gas.set_refunded(
+            result.gas.refunded().saturating_add(i64::try_from(regular_refund).unwrap_or(i64::MAX)),
+        );
+    }
 
     settle_gas(
         req.host,
@@ -106,7 +149,7 @@ pub(super) fn handle<T: EvmTypes>(
         gas_price,
         tx.gas_limit,
         floor_gas,
-        initial_state_gas,
+        settlement_initial_state_gas,
         state_refund,
         false,
         result,
@@ -114,7 +157,12 @@ pub(super) fn handle<T: EvmTypes>(
 }
 
 fn eip7702_authorization_gas<'a, T: EvmTypes>(host: &Evm<'a, T>, authorizations: usize) -> u64 {
-    let per_auth = u64::from(host.version().gas_params.get(GasId::TxEip7702PerEmptyAccountCost));
+    let mut per_auth =
+        u64::from(host.version().gas_params.get(GasId::TxEip7702PerEmptyAccountCost));
+    if host.feature(EvmFeatures::EIP2780) {
+        per_auth = per_auth
+            .saturating_sub(u64::from(host.version().gas_params.get(GasId::TxEip7702AuthRefund)));
+    }
     (authorizations as u64).saturating_mul(per_auth)
 }
 
@@ -179,6 +227,118 @@ fn apply_one_auth<'a, T: EvmTypes>(
     Ok(Some(AppliedAuth { existed, delegated_before_tx, delegated_now, clearing }))
 }
 
+#[derive(Clone, Copy, Debug, Default)]
+struct RuntimeAuthListGas {
+    state_gas: u64,
+    out_of_gas: bool,
+}
+
+fn runtime_oog_result<T: EvmTypes>(tx_gas_limit: u64, reservoir: u64) -> MessageResult<T> {
+    MessageResult {
+        stop: InstrStop::OutOfGas,
+        gas: GasTracker::new_spent_with_reservoir(tx_gas_limit, reservoir),
+        ..MessageResult::default()
+    }
+}
+
+const fn spend_regular(gas: &mut GasTracker, cost: u64) -> bool {
+    let mut next = *gas;
+    if next.spend(cost).is_err() {
+        return false;
+    }
+    *gas = next;
+    true
+}
+
+fn spend_state(gas: &mut GasTracker, cost: u64) -> bool {
+    let mut next = *gas;
+    if next.spend_state(cost).is_err() {
+        return false;
+    }
+    *gas = next;
+    true
+}
+
+fn apply_auth_list_eip2780<'a, T: EvmTypes>(
+    host: &mut Evm<'a, T>,
+    chain_id: u64,
+    authorizations: &[super::LazyAuthorization],
+    caller: Address,
+    to: TxKind,
+    value: U256,
+    gas: &mut GasTracker,
+) -> HandlerResult<RuntimeAuthListGas> {
+    let account_write = u64::from(host.version().gas_params.get(GasId::TxEip7702AuthRefund));
+    let new_account = host.version().gas_params.new_account_state_gas();
+    let auth_base = u64::from(host.version().gas_params.get(GasId::TxEip7702PerAuthState));
+    let mut written_accounts = AddressHashSet::default();
+    written_accounts.insert(caller);
+    if let TxKind::Call(target) = to
+        && !value.is_zero()
+    {
+        written_accounts.insert(target);
+    }
+    let mut charged_delegation_bytes = AddressHashSet::default();
+    let mut state_gas = 0u64;
+
+    for authorization in authorizations {
+        if !authorization.chain_id().is_zero() && authorization.chain_id() != &U256::from(chain_id)
+        {
+            continue;
+        }
+        if authorization.nonce() == u64::MAX {
+            continue;
+        }
+        let Some(authority) = authorization.authority() else {
+            continue;
+        };
+
+        let mut account = host.state.account(&authority, false).map_err(error_handler!(host))?;
+        account.warm();
+        let existed = account.exists();
+        let authority_nonce = account.nonce();
+        let code = account.load_code().map_err(error_handler!(host))?;
+        let delegated_now = !code.is_empty();
+        if delegated_now && !code.is_eip7702() {
+            continue;
+        }
+        if authorization.nonce() != authority_nonce {
+            continue;
+        }
+        let delegated_before_tx =
+            account.original_code().map_err(error_handler!(host))?.is_eip7702();
+        let clearing = authorization.address().is_zero();
+
+        if !existed {
+            if !spend_state(gas, new_account) {
+                return Ok(RuntimeAuthListGas { state_gas, out_of_gas: true });
+            }
+            state_gas = state_gas.saturating_add(new_account);
+        }
+        if !written_accounts.contains(&authority) {
+            if !spend_regular(gas, account_write) {
+                return Ok(RuntimeAuthListGas { state_gas, out_of_gas: true });
+            }
+            written_accounts.insert(authority);
+        }
+        if !clearing
+            && !delegated_now
+            && !delegated_before_tx
+            && !charged_delegation_bytes.contains(&authority)
+        {
+            if !spend_state(gas, auth_base) {
+                return Ok(RuntimeAuthListGas { state_gas, out_of_gas: true });
+            }
+            state_gas = state_gas.saturating_add(auth_base);
+            charged_delegation_bytes.insert(authority);
+        }
+
+        account.set_delegation(*authorization.address());
+    }
+
+    Ok(RuntimeAuthListGas { state_gas, out_of_gas: false })
+}
+
 /// Applies the EIP-7702 authorization list and returns `(state_refund, regular_refund)`.
 ///
 /// Follows execution-specs `set_delegation`. The per-authorization state and regular gas charged in
diff --git a/crates/evm2/src/ethereum/legacy.rs b/crates/evm2/src/ethereum/legacy.rs
index b6075df..d85f766 100644
--- a/crates/evm2/src/ethereum/legacy.rs
+++ b/crates/evm2/src/ethereum/legacy.rs
@@ -29,7 +29,13 @@ pub(super) fn handle<T: EvmTypes>(
     validate_create_initcode(req.host.version(), tx.to, &tx.input)?;
     validate_nonce_not_overflow(tx.nonce)?;
     let intrinsic = intrinsic_gas(req.host.version(), caller, tx.to, &tx.input, 0, 0, tx.value);
-    let initial_state_gas = create_initial_state_gas(req.host.version(), tx.to.is_create());
+    let create_state_gas = create_initial_state_gas(req.host.version(), tx.to.is_create());
+    let (initial_state_gas, first_frame_state_gas) =
+        if req.host.feature(crate::EvmFeatures::EIP2780) {
+            (0, create_state_gas)
+        } else {
+            (create_state_gas, 0)
+        };
     validate_intrinsic_gas(tx.gas_limit, intrinsic, initial_state_gas)?;
     let floor_gas = floor_gas(req.host.version(), &tx.input, 0, 0);
     validate_floor_gas(tx.gas_limit, floor_gas)?;
@@ -58,7 +64,15 @@ pub(super) fn handle<T: EvmTypes>(
         ..TxEnv::default()
     };
     let (bytecode, mut message) = initial_message(
-        req.host, caller, tx.nonce, tx.to, &tx.input, tx.value, gas_limit, reservoir,
+        req.host,
+        caller,
+        tx.nonce,
+        tx.to,
+        &tx.input,
+        tx.value,
+        gas_limit,
+        reservoir,
+        first_frame_state_gas,
     )?;
     let mut result = req.host.execute_message(&tx_env, bytecode, &mut message);
     rollback_failed_execution(req.host, execution_checkpoint, &mut result);
diff --git a/crates/evm2/src/ethereum/mod.rs b/crates/evm2/src/ethereum/mod.rs
index f3e200f..05614dc 100644
--- a/crates/evm2/src/ethereum/mod.rs
+++ b/crates/evm2/src/ethereum/mod.rs
@@ -482,6 +482,7 @@ pub(crate) fn initial_message<'a, T: EvmTypes>(
     value: U256,
     gas_limit: u64,
     reservoir: u64,
+    first_frame_state_gas: u64,
 ) -> HandlerResult<(Bytecode, Message<T>)> {
     let r = match to {
         TxKind::Call(to) => {
@@ -491,6 +492,7 @@ pub(crate) fn initial_message<'a, T: EvmTypes>(
                 depth: 0,
                 gas_limit,
                 reservoir,
+                first_frame_state_gas: 0,
                 destination: to,
                 caller,
                 input: input.clone(),
@@ -511,6 +513,7 @@ pub(crate) fn initial_message<'a, T: EvmTypes>(
                 depth: 0,
                 gas_limit,
                 reservoir,
+                first_frame_state_gas,
                 destination: address,
                 caller,
                 input: input.clone(),
@@ -810,8 +813,11 @@ mod tests {
         registry::TxRegistry,
     };
     use alloc::vec;
-    use alloy_consensus::{TxEip2930, transaction::Recovered};
-    use alloy_eips::eip2930::AccessList;
+    use alloy_consensus::{TxEip1559, TxEip2930, TxEip7702, TxLegacy, transaction::Recovered};
+    use alloy_eips::{
+        eip2930::AccessList,
+        eip7702::{Authorization, RecoveredAuthority, RecoveredAuthorization},
+    };
 
     #[test]
     fn intrinsic_gas_charges_shanghai_create_initcode_words() {
@@ -914,6 +920,127 @@ mod tests {
         );
     }
 
+    #[test]
+    fn amsterdam_create_state_gas_out_of_gas_is_runtime_failure() {
+        let caller = Address::with_last_byte(0xaa);
+        let mut database = InMemoryDB::default();
+        database.insert_account_info(
+            &caller,
+            AccountInfo::default().with_balance(U256::from(1_000_000_000u64)),
+        );
+        let tx = RecoveredTxEnvelope::Legacy(Recovered::new_unchecked(
+            TxLegacy {
+                gas_limit: 100_000,
+                to: TxKind::Create,
+                input: Bytes::from_static(&[op::STOP]),
+                ..Default::default()
+            },
+            caller,
+        ));
+        let mut evm = Evm::<BaseEvmTypes>::new(
+            SpecId::AMSTERDAM,
+            BlockEnv::default(),
+            ethereum_tx_registry(SpecId::AMSTERDAM),
+            database,
+            Precompiles::base(SpecId::AMSTERDAM),
+        );
+
+        let result = evm.transact(&tx).expect("runtime OOG is an executed transaction").discard();
+        assert!(!result.status);
+        assert_eq!(result.stop, InstrStop::OutOfGas);
+        assert_eq!(result.tx_gas_used(), 100_000);
+    }
+
+    #[test]
+    fn amsterdam_typed_create_state_gas_out_of_gas_is_runtime_failure() {
+        let caller = Address::with_last_byte(0xaa);
+        let input = Bytes::from_static(&[op::STOP]);
+        let transactions = [
+            RecoveredTxEnvelope::Eip2930(Recovered::new_unchecked(
+                TxEip2930 {
+                    chain_id: 1,
+                    gas_limit: 100_000,
+                    to: TxKind::Create,
+                    input: input.clone(),
+                    ..Default::default()
+                },
+                caller,
+            )),
+            RecoveredTxEnvelope::Eip1559(Recovered::new_unchecked(
+                TxEip1559 {
+                    chain_id: 1,
+                    gas_limit: 100_000,
+                    to: TxKind::Create,
+                    input,
+                    ..Default::default()
+                },
+                caller,
+            )),
+        ];
+
+        for tx in transactions {
+            let mut database = InMemoryDB::default();
+            database.insert_account_info(
+                &caller,
+                AccountInfo::default().with_balance(U256::from(1_000_000_000u64)),
+            );
+            let mut evm = Evm::<BaseEvmTypes>::new(
+                SpecId::AMSTERDAM,
+                BlockEnv::default(),
+                ethereum_tx_registry(SpecId::AMSTERDAM),
+                database,
+                Precompiles::base(SpecId::AMSTERDAM),
+            );
+
+            let result =
+                evm.transact(&tx).expect("runtime OOG is an executed transaction").discard();
+            assert!(!result.status);
+            assert_eq!(result.stop, InstrStop::OutOfGas);
+            assert_eq!(result.tx_gas_used(), 100_000);
+        }
+    }
+
+    #[test]
+    fn amsterdam_authorization_state_gas_out_of_gas_is_runtime_failure() {
+        let caller = Address::with_last_byte(0xaa);
+        let authority = Address::with_last_byte(0xcc);
+        let mut database = InMemoryDB::default();
+        database.insert_account_info(
+            &caller,
+            AccountInfo::default().with_balance(U256::from(1_000_000_000u64)),
+        );
+        let authorization = RecoveredAuthorization::new_unchecked(
+            Authorization {
+                chain_id: U256::from(1),
+                address: Address::with_last_byte(0xdd),
+                nonce: 0,
+            },
+            RecoveredAuthority::Valid(authority),
+        );
+        let tx = LazyTxEip7702::from_cached_recovered_authorizations(
+            TxEip7702 {
+                chain_id: 1,
+                gas_limit: 100_000,
+                to: Address::with_last_byte(0xbb),
+                ..Default::default()
+            },
+            vec![authorization],
+        );
+        let tx = RecoveredTxEnvelope::Eip7702(Recovered::new_unchecked(tx, caller));
+        let mut evm = Evm::<BaseEvmTypes>::new(
+            SpecId::AMSTERDAM,
+            BlockEnv::default(),
+            ethereum_tx_registry(SpecId::AMSTERDAM),
+            database,
+            Precompiles::base(SpecId::AMSTERDAM),
+        );
+
+        let result = evm.transact(&tx).expect("runtime OOG is an executed transaction").discard();
+        assert!(!result.status);
+        assert_eq!(result.stop, InstrStop::OutOfGas);
+        assert_eq!(result.tx_gas_used(), 100_000);
+    }
+
     #[test]
     fn floor_gas_charges_prague_calldata_tokens() {
         let input = Bytes::from_static(&[0, 1, 2]);
@@ -1082,6 +1209,7 @@ mod tests {
             U256::ZERO,
             100_000,
             0,
+            0,
         )
         .unwrap();
         assert_eq!(message.destination, target);
diff --git a/crates/evm2/src/evm/mod.rs b/crates/evm2/src/evm/mod.rs
index fe6e6e5..3bef09b 100644
--- a/crates/evm2/src/evm/mod.rs
+++ b/crates/evm2/src/evm/mod.rs
@@ -1009,6 +1009,19 @@ impl<'a, T: EvmTypes> Evm<'a, T> {
             message.destination = Self::derive_create_address(&bytecode, message, nonce);
         }
 
+        if is_create && message.first_frame_state_gas != 0 {
+            let mut probe = GasTracker::new_with_regular_gas_and_reservoir(
+                message.gas_limit,
+                message.reservoir,
+            );
+            if let Err(stop) = self.spend_first_frame_create_state_gas(message, &mut probe) {
+                let mut result =
+                    Self::error_message_result(stop, message.gas_limit, message.reservoir);
+                result.gas.settle_gas(result.stop);
+                return result;
+            }
+        }
+
         let mut top_frame: Option<Box<Interpreter<'frame, 'a, T>>> = None;
         let frame = match self.current_frame {
             // SAFETY: The parent frame is suspended on this call stack for the duration of the
@@ -1088,6 +1101,13 @@ impl<'a, T: EvmTypes> Evm<'a, T> {
         } else {
             false
         };
+        let mut frame_gas =
+            GasTracker::new_with_regular_gas_and_reservoir(message.gas_limit, message.reservoir);
+        if let Err(stop) = self.spend_first_frame_create_state_gas(message, &mut frame_gas) {
+            self.state.rollback(checkpoint, self.features);
+            return Self::error_message_result(stop, message.gas_limit, message.reservoir);
+        }
+
         if let Err(stop) = self.create_message_account(message) {
             self.state.rollback(checkpoint, self.features);
             return Self::error_message_result(stop, message.gas_limit, message.reservoir);
@@ -1096,10 +1116,6 @@ impl<'a, T: EvmTypes> Evm<'a, T> {
         message.disable_precompiles = false;
         let input = core::mem::take(&mut message.input);
 
-        // Creates pay their NEW_ACCOUNT-equivalent state gas upfront via the tx-level
-        // `initial_state_gas`, so the create frame starts from the inherited gas as-is.
-        let frame_gas =
-            GasTracker::new_with_regular_gas_and_reservoir(message.gas_limit, message.reservoir);
         let stop = self.run_interpreter(bytecode, tx_env, message, frame_gas);
         message.input = input;
 
@@ -1164,6 +1180,27 @@ impl<'a, T: EvmTypes> Evm<'a, T> {
         }
     }
 
+    fn account_is_empty(&mut self, address: &Address) -> Result<bool, InstrStop> {
+        match self.state.account(address, false) {
+            Ok(account) => Ok(account.get().is_none_or(AccountInfo::is_empty)),
+            Err(code) => Err(store_error!(self, code)),
+        }
+    }
+
+    fn spend_first_frame_create_state_gas(
+        &mut self,
+        message: &Message<T>,
+        gas: &mut GasTracker,
+    ) -> Result<(), InstrStop> {
+        if message.first_frame_state_gas == 0 {
+            return Ok(());
+        }
+        if self.account_is_empty(&message.destination)? {
+            gas.spend_state(message.first_frame_state_gas)?;
+        }
+        Ok(())
+    }
+
     #[inline(never)]
     fn create_message_account(&mut self, message: &Message<T>) -> Result<(), InstrStop> {
         self.state
@@ -1985,6 +2022,7 @@ mod tests {
             depth: 0,
             gas_limit: 30_000,
             reservoir: 0,
+            first_frame_state_gas: 0,
             destination: address,
             caller: Address::ZERO,
             input: Bytes::new(),
@@ -2472,6 +2510,7 @@ mod tests {
             depth: 67,
             gas_limit: 30_000,
             reservoir: 0,
+            first_frame_state_gas: 0,
             destination: address,
             caller: Address::with_last_byte(0x7a),
             input: Bytes::from_static(b"message input"),
diff --git a/crates/evm2/src/evm/system.rs b/crates/evm2/src/evm/system.rs
index 9aa35d4..e91aaf2 100644
--- a/crates/evm2/src/evm/system.rs
+++ b/crates/evm2/src/evm/system.rs
@@ -135,6 +135,7 @@ impl<'a, T: EvmTypes> Evm<'a, T> {
             U256::ZERO,
             SYSTEM_CALL_GAS_LIMIT,
             reservoir,
+            0,
         ) {
             Ok(result) => result,
             Err(error) => {
diff --git a/crates/evm2/src/interpreter/instructions/system.rs b/crates/evm2/src/interpreter/instructions/system.rs
index c3635c0..1efb3db 100644
--- a/crates/evm2/src/interpreter/instructions/system.rs
+++ b/crates/evm2/src/interpreter/instructions/system.rs
@@ -204,6 +204,7 @@ fn prepare_call<T: EvmTypesHost>(
         depth: current.depth.saturating_add(1),
         gas_limit,
         reservoir: gas.reservoir(),
+        first_frame_state_gas: 0,
         destination,
         caller,
         input,
@@ -334,6 +335,7 @@ fn create_inner<T: EvmTypesHost>(
         depth: current.depth.saturating_add(1),
         gas_limit,
         reservoir: gas.reservoir(),
+        first_frame_state_gas: 0,
         destination: current.destination,
         caller: current.destination,
         input,
diff --git a/crates/evm2/src/interpreter/message.rs b/crates/evm2/src/interpreter/message.rs
index 2999d02..6e1b006 100644
--- a/crates/evm2/src/interpreter/message.rs
+++ b/crates/evm2/src/interpreter/message.rs
@@ -45,6 +45,11 @@ pub struct Message<T: EvmTypesHost = BaseEvmTypes> {
     /// [`GasTracker::merge_child_gas`](crate::interpreter::GasTracker::merge_child_gas).
     /// Zero for non-Amsterdam execution.
     pub reservoir: u64,
+    /// EIP-2780/EIP-8037 state gas charged on the first frame after transaction inclusion.
+    ///
+    /// Used for top-level CREATE state gas so failure can roll the charge back through normal
+    /// frame gas settlement. Zero for non-top-level messages and non-Amsterdam execution.
+    pub first_frame_state_gas: u64,
     /// Account whose context is being executed.
     pub destination: Address,
     /// Immediate caller.
diff --git a/crates/jit/builtins/src/lib.rs b/crates/jit/builtins/src/lib.rs
index 8ae87cc..90c9e6a 100644
--- a/crates/jit/builtins/src/lib.rs
+++ b/crates/jit/builtins/src/lib.rs
@@ -686,6 +686,7 @@ pub unsafe extern "C" fn __revmc_builtin_create(
         depth: current.depth.saturating_add(1),
         gas_limit,
         reservoir: ecx.gas.reservoir(),
+        first_frame_state_gas: 0,
         destination: current.destination,
         caller: current.destination,
         input: code,
@@ -789,6 +790,7 @@ pub unsafe extern "C" fn __revmc_builtin_call(
         depth: current.depth.saturating_add(1),
         gas_limit,
         reservoir: ecx.gas.reservoir(),
+        first_frame_state_gas: 0,
         destination,
         caller,
         input,
-- 
2.43.0

