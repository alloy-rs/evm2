# Fuzzer Triage Ledger

Last updated: 2026-07-13

This file tracks differential-fuzzer crashes that were triaged as either real bugs or benign compatibility gaps. "Patched over" means the fuzzer comparator normalizes the difference so future runs can keep finding new issues. It does not mean evm2 execution was changed.

## Active Bugs

| ID | Bucket | Root Cause | Impact | Patched over as benign? | Evidence |
| --- | --- | --- | --- | --- | --- |
| `REVM_EIP7708_INSPECTOR_LOG_HOOK` | `lifecycle_compare_amsterdam` | revm creates EIP-7708 ETH transfer logs inside the journal for frame-init value transfers and SELFDESTRUCT, but the inspector path only forwarded LOG opcode logs and precompile logs. Journal-created Amsterdam transfer logs could therefore appear in receipts while missing from `Inspector::log` callbacks. | Real tracer/consumer impact for consumers using revm inspectors on Amsterdam: ETH transfer events can be missing from traces even though final execution and receipts are correct. Execution/state comparison is not affected. | No. The fuzzer-side EIP-7708 transfer-log normalizer was removed; the intended fix is in revm. | Current fuzzing branch pins `0xalpharush/revm:agent/eip7708-inspector-logs` at `825a01ff`, which includes this `f44e4d8c` fix. Draft PR [bluealloy/revm#3796](https://github.com/bluealloy/revm/pull/3796) now carries `f44e4d8c`, `b40ba9af`, and `825a01ff` on `a0b11be8` (`rakita/eip2780-runtime-gas-phase`); `fuzz/revm-eip7708-inspector.patch` applies cleanly. Focused `revm-inspector` tests `eip7708` and `selfdestruct_to_self` pass on the PR head. |
| `REVM_STALE_SELFDESTRUCT_JOURNAL_HOOK` | `structured_compare_{cancun,prague}`, `lifecycle_compare_{cancun,prague,osaka,amsterdam}` | After a `SELFDESTRUCT`, revm derived inspector metadata from `journal.last()`. Cancun and later make `SELFDESTRUCT` to the executing account a no-op for an existing contract, so the opcode adds no destruction/transfer journal entry. revm then reused an unrelated earlier `AccountDestroyed` or `BalanceTransfer`, commonly the transaction value transfer, and emitted a fabricated callback with stale contract, beneficiary, and value. | Real tracer/API impact for revm consumers: traces can report a SELFDESTRUCT by the wrong account, to the wrong beneficiary, for the wrong amount. Execution, state, and receipts are unaffected. | No. The comparator continues to compare metadata whenever both engines emit a hook. revm now records the pre-instruction journal index and inspects only entries created by that opcode. | Draft PR [bluealloy/revm#3797](https://github.com/bluealloy/revm/pull/3797) contains commit `dcc55cd6` on local [`agent/selfdestruct-inspector-journal`](https://github.com/bluealloy/revm/pull/3797), based on revm `main` `a094a93f`; regression `cancun_selfdestruct_to_self_does_not_reuse_prior_journal_entry`. Seventeen recent artifacts across six buckets pass after the fix; grouped replay logs: `/tmp/evm2-recent-20260713-fixed2/`. |
| `REVM_AMSTERDAM_SELFDESTRUCT_SELF_VALUE` | `structured_compare_amsterdam` | The stale-journal fix makes revm inspect the correct `AccountDestroyed` entry, but Amsterdam/EIP-8246 selfdestruct-to-self preserves the account balance and stores `had_balance = 0` for revert bookkeeping. revm forwards `had_balance` as the `Inspector::selfdestruct` value, so a newly-created contract that receives create value and then selfdestructs to itself reports value `0` instead of the preserved balance. | Real tracer/API impact for revm consumers on Amsterdam: SELFDESTRUCT callbacks can under-report the value associated with the opcode even though execution, state, and receipts match. | No. The comparator continues to compare SELFDESTRUCT metadata whenever both engines emit a hook. The intended fix is in revm: when the correct `AccountDestroyed` entry is self-targeted with zero `had_balance`, read the preserved account balance from journal state for the callback value. | Reproduces with `fuzz/artifacts/structured_compare_amsterdam/crash-97865b0ccb6eca1d7c1c70cbe3229bbdb91d8ce6`: revm reports `value=0`, evm2 reports `value=304` for initcode `ADDRESS ADDRESS SELFDESTRUCT STOP`. Patch fallback `fuzz/revm-amsterdam-selfdestruct-value.patch` contains revm commit `825a01ff` on top of `b40ba9af`; focused tests `amsterdam_selfdestruct_to_self_reports_preserved_balance` and `cancun_selfdestruct_to_self_does_not_reuse_prior_journal_entry` pass in `/tmp/revm-amsterdam-selfdestruct-inspector-value`. The helper branch PR [0xalpharush/revm#1](https://github.com/0xalpharush/revm/pull/1) was merged into `agent/eip7708-inspector-logs`, the fuzz workspace pins that PR 3796 branch, and fixed replay of this artifact passes. |

## Fixed Locally

| ID | Bucket | Root Cause | Impact | Patched over as benign? | Evidence |
| --- | --- | --- | --- | --- | --- |
| `AMSTERDAM_FLOOR_DECOMPOSED_BASE` | `structured_compare_amsterdam` | EIP-2780 replaces the legacy 21,000 transaction-floor base with the transaction's full decomposed regular-gas base. evm2 configured `TxFloorCostBase` to only the 12,000 sender charge and `floor_gas()` had no sender, destination, or value inputs, so it omitted recipient access and value-transfer components. A distinct-recipient value transfer with one zero calldata byte therefore used a 12,064 floor instead of 21,064; actual execution stopped at 21,014, undercharging 50 gas. | Real execution, fee, receipt, and balance mismatch for Amsterdam consumers whenever the correct decomposed floor exceeds spent gas. The reproduced transaction charged 50 too little gas, refunded the sender 100 wei too much at gas price 2, and paid the beneficiary 50 wei too little. | No. evm2 now derives the floor base from sender, destination/create kind, and value for every transaction envelope; pre-Amsterdam fixed-base behavior is unchanged. | Fixed replay: `fuzz/artifacts/structured_compare_amsterdam/crash-0b79b0d119b5490aa229b389dd8721392c5e1438`. Regression `amsterdam_floor_uses_decomposed_value_transfer_base`; current-branch Amsterdam tests (10) and warnings-denied evm2 Clippy pass. Standalone latest-main branch `0xalpharush/evm2:agent/amsterdam-floor-decomposed-base` at `629b2e5`, one commit ahead and zero behind `upstream/main` `a428621`; patch fallback under `fuzz/evm2-amsterdam-floor-decomposed-base-patches/` applies cleanly. |
| `AMSTERDAM_RUNTIME_GAS_CREATE_OOG` | `structured_compare_amsterdam` | Amsterdam/EIP-2780 moved state-dependent transaction charges, including top-level create state gas, out of intrinsic validity and into a runtime gas phase. revm reports low-gas create as an included out-of-gas halt, while evm2 rejected pre-execution with `IntrinsicGasTooLow` because `create_initial_state_gas` was included in `validate_intrinsic_gas`. | Real execution mismatch. Consumers executing Amsterdam create transactions could get different inclusion, nonce, gas charging, and receipt behavior from revm/current spec branch. | No. This was fixed in evm2 rather than normalized. | Local evm2 changes move top-level create state gas into first-frame runtime gas and preflight inspected create OOG before emitting a frame. Fixed replays now pass for `fuzz/artifacts/structured_compare_amsterdam/crash-bf57ebb7b28ad385f6ec6247da4b2d3b00c53256`, `fuzz/artifacts/structured_compare_amsterdam/crash-a55c619e84466ef25a950f6224427bad83694664`, and `fuzz/artifacts/structured_compare_amsterdam/crash-d90a4d676c82bf727233b3f0606f699f084bf64b`. Standalone latest-main branch: `0xalpharush/evm2:agent/amsterdam-runtime-gas` at `0412a6c`, one commit ahead of fetched `upstream/main` `a428621`; clean worktree `/tmp/evm2-amsterdam-pr`. Validated there with `cargo test -p evm2 amsterdam --lib` (8 tests, including the create and authorization runtime-OOG regressions) and `LLVM_SYS_221_PREFIX=/usr/lib/llvm-22 cargo clippy -p evm2 -p evm2-jit-builtins --all-targets -- -D warnings`. Baseline revm PR: `bluealloy/revm#3789` / branch `rakita/eip2780-runtime-gas-phase`. Patch fallback exported as `fuzz/evm2-amsterdam-runtime-gas.patch`. |
| `AMSTERDAM_RUNTIME_GAS_CALL_OOG_INSPECTOR` | `structured_compare_amsterdam` | `execute_message_inspected` fired the top-level `Inspector::call` hook before `execute_call_message` charged Amsterdam/EIP-2780 depth-0 call runtime gas. A value transfer to an empty recipient that could afford intrinsic gas but not `new_account_state_gas` therefore produced an evm2 call frame and EIP-7708 transfer-log callback before halting, while revm pre-execution sets `runtime_oog` before the first frame is entered. | Real tracer impact for Amsterdam consumers using evm2 inspectors: a transaction-level runtime-OOG value call could appear as a failed call frame with a transfer log callback even though the transfer/log is rolled back and absent from committed execution output. Execution/state comparison already passed because evm2 rolled the journal back. | No. This was fixed in evm2 by preflighting depth-0 call runtime gas before inspected call hooks. | Fixed replay passes for `fuzz/artifacts/structured_compare_amsterdam/crash-dc81cee312825a7f24931cb945e94e42b780b134`; original raw diff was revm `events=[]` vs evm2 `CallStart -> EIP-7708 Log -> CallEnd(Halt)`. Covered by `evm::tests::amsterdam_runtime_oog_call_value_is_not_inspected`. Standalone latest-main branch: `0xalpharush/evm2:agent/amsterdam-call-runtime-oog-inspector` at `4775db1`, one commit ahead of fetched `upstream/main` `a428621`; clean worktree `/tmp/evm2-call-runtime-oog-inspector`. The focused regression test and `cargo clippy -p evm2 --all-targets -- -D warnings` pass there. Patch fallback exported as `fuzz/evm2-amsterdam-call-runtime-oog-inspector.patch`. |
| `AMSTERDAM_EIP7702_RUNTIME_AUTH_GAS` | `structured_compare_amsterdam` | Amsterdam/EIP-2780 moves EIP-7702 authority `ACCOUNT_WRITE` from pessimistic intrinsic/refund accounting into the runtime authorization phase. revm only charges the 8,000 regular-gas `ACCOUNT_WRITE` when the auth actually writes an authority whose write was not already paid. evm2 charged `TxEip7702PerEmptyAccountCost` up front and later refunded the existing-authority 8,000 through the transaction refund counter, so the top-level call frame started with 8,000 less visible gas. | Real tracer-visible gas mismatch, and potentially execution-visible for tight Amsterdam EIP-7702 transactions because the first frame had less spendable regular gas in evm2. | No. This was fixed in evm2 rather than normalized. | Local evm2 changes apply EIP-7702 Amsterdam auth costs in runtime gas. Fixed replay now passes for `fuzz/artifacts/structured_compare_amsterdam/crash-79ac80604ce02eea32fd576658dba48cbccf0e21`; original first inspector diff was revm call gas `16744120` vs evm2 `16736120`. Standalone latest-main branch: `0xalpharush/evm2:agent/amsterdam-runtime-gas` at `0412a6c`, one commit ahead of fetched `upstream/main` `a428621`; clean worktree `/tmp/evm2-amsterdam-pr`. Validated there with `cargo test -p evm2 amsterdam --lib` (8 tests, including the create and authorization runtime-OOG regressions) and `LLVM_SYS_221_PREFIX=/usr/lib/llvm-22 cargo clippy -p evm2 -p evm2-jit-builtins --all-targets -- -D warnings`. Patch fallback exported as `fuzz/evm2-amsterdam-runtime-gas.patch`. |
| `AMSTERDAM_EIP8037_CREATE_PREACCESS_INSPECTOR` | `bytecode_compare_amsterdam` | For Amsterdam/EIP-8037 nested CREATE, revm performs the caller balance and nonce pre-access checks in the CREATE opcode before constructing `FrameInput::Create`. If the endowment is unaffordable or the creator nonce is `u64::MAX`, revm pushes `0`, clears returndata, and no create frame/inspector hook exists. evm2 charged CREATE state gas and constructed a child `Message` first, so `execute_message_inspected` emitted a failed create frame before `prepare_create_message` returned `OutOfFunds`/`Return`. | Real tracer/API mismatch for Amsterdam consumers using evm2 inspectors: an unaffordable CREATE could appear as a failed child create frame even though revm/current Amsterdam semantics never enter a create frame. Execution output still pushed `0`, but trace consumers would see a spurious create attempt and frame result. | No. This was fixed in evm2 by moving Amsterdam CREATE balance/nonce prechecks and conditional create-state-gas charging into the opcode path before child message construction. | Fixed replay passes for `fuzz/artifacts/bytecode_compare_amsterdam/crash-3e713f478b7b265d09ff8de498680ae3a98fd9f9`; original diff was revm top-level frame with 5 events vs evm2 with an extra `Create(kind=Create, caller=0x2020..., value=13805262, gas_limit=9637414)` child frame. Covered by `evm::inspector::tests::amsterdam_create_out_of_funds_does_not_fire_create_hook`. Standalone latest-main branch: `0xalpharush/evm2:agent/amsterdam-create-preaccess-inspector` at `2c6a3e4`, one commit ahead of fetched `upstream/main` `a428621`; clean worktree `/tmp/evm2-amsterdam-create-preaccess`. The focused regression test and `cargo clippy -p evm2 --all-targets -- -D warnings` pass there. The exact interpreter commit is shared with the combined JIT branch; its fallback is patch 1 under `fuzz/evm2-amsterdam-jit-create-preaccess-patches/`. |
| `AMSTERDAM_JIT_CREATE_PREACCESS_GAS` | `bytecode_compare_amsterdam` | The JIT `CREATE` builtin retained the old EIP-8037 path: it unconditionally charged `create_state_gas` and entered child-create handling before the Amsterdam caller balance/nonce pre-access checks and destination-emptiness test. In the crash, a memory-heavy `STATICCALL` left less than the state charge and `CREATE` used the unaffordable 160-bit `ORIGIN` as its endowment, so revm and the evm2 interpreter pushed zero and completed while JIT halted out of gas. | Real execution and fee mismatch for Amsterdam consumers using evm2 JIT: a transaction that should succeed can become an exceptional halt, consume its full gas limit, and produce different sender/beneficiary balances and state. | No. The JIT builtin now mirrors the interpreter/revm pre-access ordering and conditional state-gas charge. | Fixed replay passes for `fuzz/artifacts/bytecode_compare_amsterdam/crash-f97ed6c8940d4ebaf809c86eeb31dd32af83455e` through revm, evm2 interpreter, and JIT in 44 ms; without JIT the artifact already passed. Covered by `compiled_amsterdam_create_checks_balance_before_state_gas`. Latest-main branch `0xalpharush/evm2:agent/amsterdam-jit-create-preaccess` is based on `upstream/main` `a428621`, with interpreter prerequisite `2c6a3e4` and JIT fix `44da979`; 62 JIT runtime tests, 7 Amsterdam evm2 tests, and targeted JIT clippy with warnings denied pass. The ordered patch series applies cleanly to detached `upstream/main` `a428621`. Patch fallbacks: `fuzz/evm2-amsterdam-jit-create-preaccess-patches/`. |
| `PRAGUE_EIP7702_INSPECTOR_CODE_ADDRESS` | `structured_compare_prague` | evm2 exposed the resolved EIP-7702 delegated account in `Message.code_address`, while revm keeps `CallInputs::bytecode_address` equal to the call operand/target and carries the delegated bytecode separately as known bytecode. evm2 also used a `disable_precompiles` flag for delegated frames, which let delegated code bypass normal precompile dispatch keyed by the call target. | Real tracer/API mismatch for EIP-7702 delegated calls: inspector consumers saw the delegated code address where revm reports the target bytecode address. Also execution-visible for the edge case where a precompile target carries EIP-7702 delegation, because evm2 could run delegated bytecode instead of preserving revm precompile precedence. | No. This was fixed in evm2 and the fuzzer-side code-address normalizer was removed. | Fixed replay passes for `fuzz/artifacts/structured_compare_prague/crash-f6b48c4f7c84cc3055b45b8bb3b822cf07c3ed08`; original diff was revm `code_address=target` vs evm2 `code_address=0x0000000000000000000000000000000000000006` with matching execution/steps. Covered by `ethereum::tests::initial_delegated_call_keeps_target_code_address`; standalone latest-main branch `0xalpharush/evm2:agent/eip7702-code-address` at `aca3fca`, one commit ahead of fetched `upstream/main` `a428621`; clean worktree `/tmp/evm2-eip7702-code-address`. The focused regression test and `cargo clippy -p evm2 --all-targets -- -D warnings` pass there. Patch fallback exported as `fuzz/evm2-eip7702-code-address.patch`. |

## Benign Trace Incompatibilities

| ID | Bucket | Root Cause | Impact | Patched over as benign? | Evidence |
| --- | --- | --- | --- | --- | --- |
| `EMPTY_CALL_INITIALIZE` | `precompile_compare_*`, `lifecycle_compare_osaka`, `structured_compare_osaka`, `lifecycle_compare_amsterdam` | evm2 may emit an `initialize_interp` trace for an empty successful call frame where revm does not create an interpreter frame. In Amsterdam value-transfer calls, both backends can report the EIP-7708 transfer log while only evm2 reports the zero-length initialize hook. | Trace-shape only. Execution result/state comparison passes. | Yes. The inspector normalizer drops only `Initialize(code_len=0)` for successful empty call frames and preserves any transfer logs. | Representative artifacts `precompile_compare_prague/crash-acfec1ff15a5aa3e972407fda0101fe5c5111ddd`, `lifecycle_compare_osaka/crash-599aa90b918ba56cea4d9e7e1a1e57bae4f0761a`, `structured_compare_osaka/crash-5fed0494c6e54f09310430ffb5fdd2a7ef10f102`, and `lifecycle_compare_amsterdam/crash-ceb59ffa8e2696055815db5f84d992f61a46fc25`. |
| `PARENT_STEP_END_AROUND_CHILD_FRAME` | inspector comparisons with nested calls, including `lifecycle_compare_istanbul` | revm reports `step_end` for the parent CALL/CREATE opcode before or around the child frame because revm executes the call instruction as a complete instruction. evm2 keeps the parent opcode open while the child frame runs, so `step_end` can land after the child returns or be implied by frame structure. | Trace-shape only. evm2 ordering is intentional for richer end-of-CALL state; tracers must handle pending parent steps. | Yes. The inspector normalizer associates child-frame boundaries with the pending parent call/create step and still compares child-frame metadata. | Covered by `normalizes_parent_step_end_around_child_call_frame` and `normalizes_parent_call_step_end_when_child_frame_exists` in `crates/cli/src/fuzzer/inspect.rs`; artifact `fuzz/artifacts/lifecycle_compare_istanbul/crash-b0ed1ce4b33d8b2ee3f35e21dcb66e9e155fd206`. |
| `TERMINAL_HOOK_STEP_END` | `bytecode_compare_shanghai` | When the final executed opcode emits a side-effect hook such as `LOG0`, revm reports the hook, then `step_end`, then a synthetic EOF `STOP`; evm2 reports the hook and closes the frame without a separate `step_end`. | Trace-shape only. Execution result, receipt log, and state comparison passed before the inspector comparison failed. | Yes. The inspector normalizer marks the final active step ended when only terminal side-effect hooks trail it before frame end. | Replayed artifact `fuzz/artifacts/bytecode_compare_shanghai/crash-f368f0a60c96f861713f29756d6891543f0eebf5`; bytecode `0x323b32a0` (`ORIGIN EXTCODESIZE ORIGIN LOG0`). Covered by `normalizes_terminal_log_step_end_before_frame_end`. |
| `SYNTHETIC_EOF_STOP` | `bytecode_compare_*`, `structured_compare_*` | revm emits a synthetic `STOP` step when legacy bytecode falls off the end, when create initcode is empty, or when a truncated `PUSHn` advances pc past the original bytecode length before EOF. evm2 ends the frame without reporting that virtual opcode. | Trace-shape only. State/gas/result comparison passed for these artifacts. | Yes. The inspector normalizer records interpreter code length and drops only terminal `STOP` steps at `pc >= code_len`, so real in-bytecode `STOP` opcodes are still compared. It also marks the final evm2 step ended when frame close implies termination. | Representative replays now pass for `structured_compare_osaka/crash-4775c5ccb2a6b19ee73525e717526fbbf5a2ea3f`, `structured_compare_merge/crash-55b055f607688748a8e9ad8c09426df1a44ea7d1`, and `structured_compare_berlin/crash-ccacd9fa57f01205bb7bd438328264d236f53f06`; covered by `normalizes_revm_padded_eof_stop_after_truncated_push`. |
| `SELFDESTRUCT_HOOK_PRESENCE` | `lifecycle_compare_{cancun,osaka,prague}` | For an EIP-6780 SELFDESTRUCT-to-self no-op on a pre-existing contract, revm reconstructs its inspector callback from the last journal entry; the no-op appends no destruction/transfer entry, so revm emits no hook. evm2 reports the successfully executed SELFDESTRUCT opcode hook. | Trace-shape only for the replayed artifacts; execution result/state comparison passed. | Yes, narrowly and pairwise. At Cancun or later, only a one-sided `SelfDestruct` with `contract == target` is removed. When both backends emit hooks their full metadata remains compared; pre-Cancun and non-self one-sided hooks remain mismatches. | Representative artifacts `lifecycle_compare_cancun/crash-f48436602de6c79a2a6a45f9123d2c3f164b277b`, `lifecycle_compare_prague/crash-8baf9e44d69e2c3f37b21e1b3e9d5d48a9db6733`, and `lifecycle_compare_osaka/crash-219100ec09d568b639dcb4e6364b583c67f768e6`. Covered by four positive/negative pairwise tests; all 12 saved crashes in those buckets pass under `/tmp/evm2-pairwise-normalization-replay/summary`. |
| `FAILED_CREATE_ADDRESS_TRACE` | `evm_smith_compare_*` | revm `CreateOutcome` carries the would-be created address even when the create halts/reverts; evm2 reports `created_address: None` for non-successful creates. | Trace-shape only for the replayed artifacts; execution result/state comparison passed. | Yes, narrowly and pairwise. The address is cleared only when both results have the same non-success kind and exactly one backend reports an address. Two non-null addresses remain compared, as do successful-create addresses. | `fuzz/artifacts/evm_smith_compare_berlin/crash-da39a3ee5e6b4b0d3255bfef95601890afd80709` passes; covered by positive and negative address tests. The combined affected-bucket replay passed 13/13 artifacts under `/tmp/evm2-pairwise-normalization-replay/summary`. |
| `TRACE_CAP_TRUNCATION_BOUNDARY` | `lifecycle_compare_istanbul`, `structured_compare_spurious_dragon`, `lifecycle_compare_spurious_dragon` | Some programs can exceed the fuzzer inspector recorder cap (`MAX_TRACE_EVENTS`) in one or both backends. Once any trace is explicitly truncated, the comparator no longer has a reliable full event stream for that transaction; comparing the cutoff point, or comparing a truncated revm trace against a finite evm2 trace, only reports the recorder boundary. | Trace-comparator artifact only. Final execution comparison passed before inspector comparison; the recorder intentionally stopped collecting events. | Yes. A normalized tx containing `Truncated` is reduced to the `Truncated` marker, and the comparator now applies that marker pairwise when either backend hit the cap. Errors and tx counts are still compared. | Fixed-input replays now pass for `fuzz/artifacts/lifecycle_compare_istanbul/crash-b0ed1ce4b33d8b2ee3f35e21dcb66e9e155fd206`, `fuzz/artifacts/lifecycle_compare_istanbul/crash-4df49c8772edb11b686a043cb5634438e25b2d2f`, and `fuzz/artifacts/structured_compare_spurious_dragon/crash-2ca3d69ff0c6bd303acaf7b8647d0d2188812855`; the saved `structured_compare_spurious_dragon/crash-*` and `lifecycle_compare_spurious_dragon/crash-*` buckets pass. Batch replay logs: `/tmp/evm2-structured-spurious-dragon-batch.log`, `/tmp/evm2-lifecycle-spurious-dragon-batch.log`. Covered by `normalizes_one_sided_trace_truncation`. |
| `REVM_TRACE_GLOBAL_STEPPED_FLAG` | inspector harness for nested frames | The fuzzer's revm trace adapter carried one global `stepped` boolean to decide whether to record `StepEnd`. Nested interpreter frames can clobber global per-step state, while revm itself calls `step_end` directly after each executed instruction in `inspect_instructions`. | Harness-only trace risk. It does not affect revm execution or evm2 execution, but it can distort the fuzzer's baseline trace in nested calls. | Yes. The revm trace adapter now records `StepEnd` when revm calls `step_end`, without cross-frame `stepped` state. | Source path `crates/cli/src/fuzzer/inspect.rs`; local revm source `crates/inspector/src/handler.rs::inspect_instructions`. |

## Fuzzer Harness Performance Artifacts

| ID | Bucket | Root Cause | Impact | Patched over as benign? | Evidence |
| --- | --- | --- | --- | --- | --- |
| `STRUCTURED_ARBITRARY_GAS_BURN` | `structured_compare_frontier/oom-d258d0dfb2f4f570e65f82d68a5488a3738f33e1` | The arbitrary structured decoder allows up to 100,000,000 gas per transaction. This 630-byte Frontier artifact decoded to two 100M-gas legacy transactions targeting bytecode that forms a valid gas-burning loop (`JUMPDEST...GAS; JUMPDEST; SLOAD; JUMP` back to PC 0). Execution comparison was cheap, but inspector comparison must run the gas-burning trace to completion; cargo-fuzz instrumentation amplified that into an OOM/timeout-style artifact. | Fuzzer performance false positive only. The replayed case had no revm/evm2 execution or inspector mismatch, but it can consume minutes and mask semantic crashes. | Yes, as harness runner configuration rather than a semantic normalization or gas reduction. The fuzz screen launcher now passes `-timeout=300 -ignore_ooms=1 -rss_limit_mb=8192` by default, preserving the 100M arbitrary gas coverage while avoiding OOM artifacts as crash reports. | The artifact replayed at unchanged 100M gas in 248,171 ms under `-timeout=300 -rss_limit_mb=4096`, and repeatable helper replay passes with `-runs=1 -timeout=300 -ignore_ooms=1 -rss_limit_mb=8192`; log: `/tmp/evm2-replay-helper-ooms/structured_compare_frontier.log`. Trial gas caps reduced runtime but were not kept. Phase profiling via `/tmp/evm2-dumpcase` showed `compare_case` under 40 ms and `compare_inspector_case` about 5.6 s outside libFuzzer. |
| `STALE_LIFECYCLE_TIMEOUT_ARTIFACTS` | `lifecycle_compare_{berlin,merge,petersburg,shanghai,spurious_dragon}/timeout-*` | Saved timeout artifacts from an earlier fuzz run are finite lifecycle cases under the current harness and comparator. Fixed replay shows they complete; no revm/evm2 semantic or inspector mismatch is produced. | Fuzzer artifact hygiene only. These can be ignored for semantic triage unless they regress into reproducible timeouts under the current harness. | No semantic patch. They were reclassified by fixed-input replay evidence. | All five saved lifecycle `timeout-*` artifacts replay under `-timeout=300`, returning in 1.4-10.6s. Repeatable helper replay on 2026-07-10 passed 5 artifacts across 5 targets with no failures; summary: `/tmp/evm2-replay-helper-timeouts/summary`; per-target logs: `/tmp/evm2-replay-helper-timeouts/*.log`. |
| `STALE_EVM_SMITH_OOM_ARTIFACTS` | `evm_smith_compare_{amsterdam,osaka,prague}/oom-*` | Saved OOM artifacts from earlier evm-smith compare runs no longer reproduce memory exhaustion under the current harness. Two artifacts are empty inputs and the non-empty Amsterdam artifact is 52 bytes. | Fuzzer artifact hygiene only. No semantic mismatch or current memory blow-up was observed. | No semantic patch. They were reclassified by fixed-input replay evidence. | The four saved evm-smith `oom-*` artifacts replay through the helper with `-runs=1 -timeout=300 -ignore_ooms=1 -rss_limit_mb=8192` and return cleanly. The same OOM helper run also passed the separate structured Frontier gas-burn artifact; summary: `/tmp/evm2-replay-helper-ooms/summary`; evm-smith per-target logs: `/tmp/evm2-replay-helper-ooms/evm_smith_compare_*.log`. |
| `STALE_LIVE_FUZZ_SESSIONS` | Running `scripts/fuzz_screen.sh` sessions started before the current fixes | Long-running screen sessions keep their original compiled fuzz binaries and libFuzzer args. The sessions observed on 2026-07-10 were launched with only `-rss_limit_mb=8192`, so they did not inherit the current timeout/OOM defaults or later comparator and evm2 fixes. | Fuzzer operations false-positive source. Stale binaries can keep writing artifacts that no longer reproduce on the current tree. | No semantic patch. The stale sessions were stopped before starting current binaries. | In the 2026-07-10 snapshot, eight recent crash artifacts replayed cleanly and the then-current saved-crash set passed 333/333 artifacts; logs: `/tmp/evm2-recent-*.log`. |
| `OVERSUBSCRIBED_LIVE_FUZZERS` | All 91 cargo-fuzz targets launched concurrently on a 32-CPU host | The 91-way current-tree run oversubscribed CPU and build resources. LibFuzzer emitted new `slow-unit-*` files for tiny inputs while every session remained healthy and no semantic crash appeared. | Fuzzer performance noise. Slow-unit reports consume triage attention but do not establish a revm/evm2 mismatch. | Yes, as runner scheduling rather than semantic normalization. `scripts/fuzz_round_robin.sh` limits execution to eight targets, cycles all 91 targets, and sets `-report_slow_units=2147483647`; slow findings are ignored. | Deterministic testing proved bounded batching, target order, cycle wraparound, argument forwarding, and process-group cleanup. Production uses the same `--features jit --no-trace-compares`, 300-second per-input timeout, OOM handling, RSS limit, and unchanged generated gas coverage. |
| `ROUND_ROBIN_SLICE_DRIFT` | Initial `scripts/fuzz_round_robin.sh` implementation | LibFuzzer `-max_total_time` does not provide a hard wall-clock slice while loading a large seed corpus. With 20k-28k files per bytecode corpus, all eight workers remained in batch 1 after 11m26s despite a nominal 600-second slice. | Scheduling fairness and throughput issue only; no semantic mismatch or artifact appeared. Without a hard bound, later targets can be starved by corpus initialization or one slow input. | Yes, as a runner fix. `fuzz_screen.sh` now supports `FUZZ_WORKER_WALL_TIME`, wraps cargo-fuzz in GNU `timeout`, and normalizes only timeout status 124 as a clean scheduled stop; other nonzero exits still trigger crash handling. | Two-second real-worker and three-target rotation checks passed, and a 600-second production trial handed all eight workers to batch 2 exactly on schedule with clean status. A census of all 91 corpora found 471-30,720 files per target (median 6,468; p75 11,829; p90 26,140), so current production uses hard 3,600-second slices to amortize corpus initialization while retaining the eight-worker cap. It launched at `2026-07-10T13:50:27+00:00`. |
| `ROUND_ROBIN_REPLACE_ORPHANS` | `scripts/fuzz_round_robin.sh --replace` | Workers are launched in their own `setsid` sessions, but GNU `timeout` uses a separate process group. Cleanup initially signaled only the worker shell's process group, so the shell could exit while timeout/libFuzzer descendants remained orphaned under PID 1. Rapid replacement also let the exiting old scheduler overwrite the new scheduler's state file with `stopped`. | Runner contention and artifact-quality risk only. Orphaned old binaries could raise concurrency above eight and rediscover already-fixed crashes; EVM execution semantics were unaffected. | Yes, as a scheduler fix. Cleanup now TERM-polls and KILLs the entire worker session via `pkill/pgrep -s`; replacement waits for the recorded scheduler PID, and an exiting scheduler writes `stopped` only while it still owns the state file. | In the 2026-07-10 validation, all eight pre-replace PIDs exited, historical scheduler PID `1799610` retained `running` state, and exactly eight fresh timeout-wrapped workers remained. The production launch under test was `2026-07-10T15:06:38+00:00`. |



## PR Readiness

Authoritative bases fetched on 2026-07-13: evm2 `upstream/main` is `a428621afa3b441f10346e278c55430c8b49b4c6`; revm `origin/main` is `a094a93fd3fd69749a9b41c3bfaec2b2d3120b40`; the Amsterdam revm base `origin/rakita/eip2780-runtime-gas-phase` is `a0b11be8083b2082cd33b5ffa4ddabddcc42fd1e`.

| Repository | Intended base | Local branch | PR commits | Validation | Remaining blocker |
| --- | --- | --- | --- | --- | --- |
| evm2 | `main` (`a428621`) | [`agent/amsterdam-runtime-gas`](https://github.com/alloy-rs/evm2/pull/283) | `0412a6c` | Clean, 0 behind/1 ahead; formatting, 8 Amsterdam tests, warnings-denied evm2/JIT-builtins Clippy, 4 relevant fixed-input replays, and patch apply check pass. Includes legacy, EIP-2930, EIP-1559, and EIP-7702 regression coverage. | Published; draft PR opened. |
| evm2 | `main` (`a428621`) | [`agent/amsterdam-call-runtime-oog-inspector`](https://github.com/alloy-rs/evm2/pull/284) | `4775db1` | Clean, 0 behind/1 ahead; formatting, focused inspector regression, warnings-denied Clippy, replay, and patch apply check pass. | Published; draft PR opened. |
| evm2 | `main` (`a428621`) | [`agent/amsterdam-jit-create-preaccess`](https://github.com/alloy-rs/evm2/pull/285) | `2c6a3e4`, `44da979` | Clean, 0 behind/2 ahead; interpreter and compiled regressions, formatting, warnings-denied evm2/JIT Clippy, both saved replays, and ordered patch apply check pass. | Published; draft PR opened. |
| evm2 | `main` (`a428621`) | [`agent/eip7702-code-address`](https://github.com/alloy-rs/evm2/pull/286) | `aca3fca` | Clean, 0 behind/1 ahead; ordinary delegation, delegated-precompile precedence, and compiled nested inspector regressions pass; formatting, warnings-denied touched-package Clippy, replay, and patch apply check pass. | Published; draft PR opened. |
| evm2 | `main` (`a428621`) | [`agent/amsterdam-floor-decomposed-base`](https://github.com/alloy-rs/evm2/pull/287) | `629b2e5` | Clean, 0 behind/1 ahead; helper and end-to-end EIP-2930 regressions, formatting, warnings-denied Clippy, replay, and patch apply check pass. | Published; draft PR opened. |
| revm | `rakita/eip2780-runtime-gas-phase` (`a0b11be8`) | [`agent/eip7708-inspector-logs`](https://github.com/bluealloy/revm/pull/3796) | `f44e4d8c`, `b40ba9af`, `825a01ff` | Clean, 0 behind/3 ahead; focused `revm-inspector` filters `eip7708` and `selfdestruct_to_self` pass, representative replays pass, and patch fallbacks exist for the EIP-7708 log hook and Amsterdam selfdestruct value fixes. | Published; draft PR opened and description updated with the expanded inspector scope. |
| revm | `main` (`a094a93f`) | [`agent/selfdestruct-inspector-journal`](https://github.com/bluealloy/revm/pull/3797) | `dcc55cd6` | Clean, 0 behind/1 ahead; current-main regression, formatting, warnings-denied `revm-inspector` Clippy, all 17 saved replays, and patch apply check pass. | Published; draft PR opened. |

The standalone label `agent/amsterdam-create-preaccess-inspector` points at the exact shared interpreter commit `2c6a3e4` and is retired as an independent PR. The combined JIT branch adds only `44da979`; there is no duplicate interpreter commit. `AMSTERDAM_RUNTIME_GAS_CREATE_OOG` and `AMSTERDAM_EIP7702_RUNTIME_AUTH_GAS` intentionally share one execution-phase PR. CALL inspector ordering, floor accounting, and EIP-7702 code-address behavior remain separate reviewable changes.

The revm fixes are split by base and scope: the Amsterdam inspector fixes that depend on the Amsterdam runtime-gas branch are grouped in PR 3796, while the current-main stale SELFDESTRUCT metadata fix remains isolated in PR 3797. The helper PR `0xalpharush/revm#1` has been merged into PR 3796's head branch and is no longer a separate upstream review target. The old duplicate `fuzz/revm-inspector-fixes/` series and standalone CREATE patch were removed. No genuine semantic mismatch is normalized by these fixes; existing benign trace normalizations remain limited to the rows above.

## Current Verification

- Final fallback layout: one patch each for runtime gas, CALL inspector OOG, EIP-7702 code address, revm EIP-7708, revm stale SELFDESTRUCT, and revm Amsterdam selfdestruct value; one floor patch directory; and the ordered two-patch CREATE/JIT directory. Every export passes `git apply --check` on its documented base, including `fuzz/revm-amsterdam-selfdestruct-value.patch` against `b40ba9af`.
- Fixed-input replay on 2026-07-13 passed all directly relevant artifacts with JIT, `-runs=1 -timeout=300 -ignore_ooms=1 -rss_limit_mb=8192`: 6 Amsterdam structured gas/floor artifacts, 2 Amsterdam CREATE artifacts, 1 Prague EIP-7702 artifact, 1 representative EIP-7708 artifact, and all 17 stale SELFDESTRUCT artifacts. No open-ended fuzzer was run.
- Fresh fixed-input replay of `structured_compare_amsterdam/crash-97865b0ccb6eca1d7c1c70cbe3229bbdb91d8ce6` now passes with the current fuzz pin at `0xalpharush/revm:agent/eip7708-inspector-logs#825a01ff`; the previous `value=0` vs `value=304` mismatch is no longer rediscovered locally.
- Recent-crash rescan at `2026-07-13T20:03:35Z` found no `crash-*`, `oom-*`, or `timeout-*` artifacts newer than the current-pin scheduler restart. The newest non-slow artifact is the known stale standalone-session Amsterdam selfdestruct value crash `structured_compare_amsterdam/crash-97865b0ccb6eca1d7c1c70cbe3229bbdb91d8ce6` at `2026-07-13T19:25:22.795232883Z`, and it replays cleanly with the current pin.
- Saved artifact inventory is `crash-*` = 353, `timeout-*` = 5, `oom-*` = 5, and `slow-unit-*` = 662. The newest artifact of any kind is a pre-restart slow-unit at `2026-07-13T20:03:24.307273144Z`; no post-restart artifact has appeared yet.
- The round-robin scheduler remains running under PID `3015852`, launched `2026-07-13T20:03:35+00:00`, with 8 workers, 3,600-second hard slices, all 91 targets, unchanged gas generation, `--features jit --no-trace-compares`, `-timeout=300 -ignore_ooms=1 -rss_limit_mb=8192`, and slow-unit reporting suppressed. The stale standalone 91-way screen sessions were stopped, leaving only `evm2-fuzz.round-robin`.
- The fuzz workspace now pins the pushed combined revm PR branch `0xalpharush/revm:agent/eip7708-inspector-logs#825a01ff`, so current local replays include the EIP-7708 log hook fix, stale SELFDESTRUCT journal fix, and Amsterdam selfdestruct-to-self value fix from the same branch used by bluealloy/revm#3796.
- Add new rows whenever a crash is classified, including whether it was normalized or fixed in an implementation.
